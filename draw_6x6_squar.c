#include <stdio.h> // this code draw a squar with LxW
int main()
{
                 int i,j,c,l,w;
                 printf("Enter the length : ");
                 scanf("%d",&l);
                 printf("Enter the width : ");
                 scanf("%d",&w);
                 __GCC_HAVE_SYNC_COMPARE_AND_SWAP_88
                  (i=1;i<=l;i++){
                      printf("\n");
                      for(j=1;j<=w;j++){
                          printf("*");
                          
}}
return 0;
}